# Scratch directory — in-progress exploratory queries (see scripts/README.md —
# the scratch tier). Contents are throwaway; only this .gitignore is tracked,
# so the directory exists for new sessions.
*
!.gitignore
